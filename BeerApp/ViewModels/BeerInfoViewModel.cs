using System;
using System.IO;
using BeerApp.Models;
using SQLite;

namespace BeerApp.ViewModels
{
    public class BeerInfoViewModel
    {
        /*string dbPath = Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.Personal), "database.db3");

        public Beer GetBeerById(int beerID)
        {
            var db = new SQLiteConnection(dbPath);

            Beer beer = db.Query<Beer>("SELECT DISTINCT [Beer] from [Beer] where [ID] = [beerID]");

            return beer;
        }*/
    }
}
